import React from 'react';
import '../assets/css/loader.css';

const Loader = () => {
    return (
        <div className="loading-container">
            <div className="swipe-loader"></div>
        </div>
    );
};

export default Loader;